#include <bits/stdc++.h>

using namespace std;
int main()
{
    char s[100]="em yeu truong em";
    s[0]=toupper(s[0]);
    // while (strstr(s," ")!=NULL)
    // {
    //     s=strstr(s," ")+1;
    //     s[0]=toupper(s[0]);
    // }
    cout<<s;
}