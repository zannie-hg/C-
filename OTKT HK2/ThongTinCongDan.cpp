#include <bits/stdc++.h>
using namespace std;
int main()
{
    
    int n;
    cin>>n;
    char hoten[100];
    char gioitinh[100];
    char namsinh[100];
    char quequan[100];
    for (int i = 0; i < n; i++)
    {
        scanf("%s%s%s%s",hoten, gioitinh, namsinh, quequan);
        // printf("%s%s%s%s",hoten, gioitinh, namsinh, quequan);
        gets(hoten);
        gets(gioitinh);
        gets(namsinh);
        gets(quequan);

    }
    
    return 0;
}